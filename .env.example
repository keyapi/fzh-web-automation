# fzh-web-automation 环境变量模板
# 复制为 .env 后填写实际值（.env 已被 .gitignore 排除）

# --- 通途 ERP ---
# 自动登录凭据（首次运行或 cookie 过期时使用 ddddocr 识别验证码登录）
TONGTU_USER=你的通途账号邮箱
TONGTU_PASSWORD=你的通途密码

# 账号密码通过浏览器手动登录一次后，cookie 自动持久化到 chrome-profile/
# 之后无需再配置

# --- 赛狐 ERP ---
# 账号密码通过浏览器手动登录一次后，cookie 自动持久化到 sellfox-profile/
# 无需在此配置

# --- Playwright ---
# 浏览器默认使用 persistent_context，无需额外配置
# MCP 模式使用独立浏览器实例
